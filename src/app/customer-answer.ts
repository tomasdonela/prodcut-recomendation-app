export class CustomerAnswer {
    subject: string;
    answer: string;

    constructor(
        subject: string,
        answer: string
    ){
        this.subject = subject;
        this.answer = answer;
    }
}